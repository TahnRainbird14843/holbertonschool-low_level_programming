#include "main.h"

/**
 * _print_rev_recursion - print a string in reverse to stdout
 * @s: string to print
 *
 * Return: 0 always
 */
int _print_rev_recursion(char *s)
{
	int i = 1;
	char *new;
	
	return (0);
}
