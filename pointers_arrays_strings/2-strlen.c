#include "main.h"

/**
 * _strlen - finds the length of a given string.
 * @s: string
 *
 * Return: length of string.
 */
int _strlen(char *s)
{
	intecount = 0;

	while (s[count] != '\0')
		count++;

	return (count);
}
